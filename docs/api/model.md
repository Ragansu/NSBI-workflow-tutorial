# Model

The core statistical model class for NSBI — analogous to a RooFit workspace model. Defines the JIT-compiled negative log-likelihood (ratio) function written using JAX and which can be used by fitting algorithms.

## Model class

::: nsbi_common_utils.model.Model
